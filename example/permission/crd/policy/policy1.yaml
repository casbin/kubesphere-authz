apiVersion: "auth.casbin.org/v1"
kind: UniversalPolicy
metadata:
  name: permission-policy1
  namespace: kubesphere-authz-system
  labels:
    model: permission
spec:
  policyItem: |
    p,my-nginx-svc,services,CREATE,deny
  

  