apiVersion: "auth.casbin.org/v1"
kind: UniversalPolicy
metadata:
  name: allowed-repo-policy1
  namespace: kubesphere-authz-system
  labels:
    model: allowed-repo
spec:
  policyItem: |
    p, default, test, allow
  

  