{
    "configurations": [
    {
        "name": "Launch Webhook",
        "type": "go",
        "request": "launch",
        "mode": "auto",
        "program": "${workspaceRoot}/cmd/webhook",
        "cwd":"${workspaceRoot}"
    }
    ]
}